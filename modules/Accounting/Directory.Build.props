<Project>
    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../../'))" />

    <PropertyGroup>
        <ProductName>Accounting</ProductName>
        <RootNamespace>ByteLabs.FinanceServices.$(ProductName)</RootNamespace>
    </PropertyGroup>

</Project>